# Neon Postgres connection string (optional for local UI).
# The app reads ingested World series from data/world-series.json
# when DATABASE_URL is unset. Set this to sync ingest into Neon
# and to read from Postgres in production.
DATABASE_URL=
